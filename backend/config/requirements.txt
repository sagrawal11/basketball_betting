# Core Data Science
pandas==2.3.2
numpy>=1.26.4
pyarrow>=14.0.0

# Web API
Flask>=3.0.0
flask-cors>=4.0.0
pytz>=2024.1

# Web Scraping
beautifulsoup4==4.14.2
lxml==5.3.0
# html5lib: pandas.read_html fallback parser. Without it, update_player_logs.py
# crashes on no-game / off-season dates (the nightly CI's primary failure mode).
html5lib>=1.1
requests==2.32.5

# Progress Tracking
tqdm==4.67.1

# Machine Learning
scikit-learn==1.7.2
xgboost==3.0.5
lightgbm==4.6.0
catboost==1.2.8
joblib==1.5.2

# Data Visualization (optional)
matplotlib>=3.7.0
seaborn>=0.12.0

# Cloud & Infrastructure
# redis: single pinned range (was duplicated as `redis>=5.0.0` + `redis==8.0.0`).
redis>=5.0,<6
boto3>=1.34.0
# kaggle: the nightly pipeline shells out to the `kaggle` CLI for dataset downloads.
# Without it those downloads silently fail and models train without team features.
kaggle>=1.6

# Dev / Testing
pytest>=7.4.0
