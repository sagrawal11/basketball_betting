# Keep the build context tiny — only requirements.txt is COPYed into the image.
# Code + data + artifacts arrive at runtime via the docker-compose bind mount.
data/
models/artifacts/
__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/
*.log
.DS_Store
